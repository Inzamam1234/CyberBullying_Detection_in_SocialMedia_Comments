# Cyberbullying Detection in Social Media Comments

This project detects toxic social media comments using a transformer-based model (DistilBERT) and demonstrates a browser-based interface that blurs toxic comments.